name:  build

on:
      workflow_call:
      
jobs:

      build:
            runs-on: ubuntu-latest
            steps:
                  -name :
                  run: echo "code build using docker"
