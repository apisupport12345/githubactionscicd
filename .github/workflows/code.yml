name : code

on: 
    workflow_call:

jobs:
    code-checkout:
    runs-on: ubuntu-latest
    
    steps:
        -name: clone the repo
         uses: actions/checkout@v7
        -name : print msg
          runs: echo "code cloned
